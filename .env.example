# Example environment variables
# Copy to .env for local development (never commit .env)

# Not needed for normal development
# Only used if you're testing updater locally with custom endpoints
# TAURI_UPDATER_ENDPOINTS=http://localhost:3000/latest.json
